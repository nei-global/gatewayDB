CREATE TABLE [dbo].[login_note_description] (
    [code]        VARCHAR (1)  NOT NULL,
    [description] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_login_note_description] PRIMARY KEY CLUSTERED ([code] ASC)
);

